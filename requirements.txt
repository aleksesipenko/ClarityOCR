# ClarityOCR - Core Dependencies
# Install with: pip install -r requirements.txt

# PDF to Markdown conversion (marker-pdf includes surya-ocr)
marker-pdf>=0.3.0

# PyTorch (supports CUDA for NVIDIA, MPS for Apple Silicon, CPU fallback)
# For CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For MPS (Apple Silicon): pip install torch (no special install needed)
torch>=2.0.0

# System memory monitoring (used for MPS/Apple Silicon support)
psutil>=5.9.0

# Web UI server
fastapi>=0.100.0
uvicorn>=0.23.0

# LLM post-processing (LM Studio compatible)
openai>=1.0.0

# PDF utilities
pypdfium2>=4.0.0

# Optional: Development dependencies
# ruff>=0.1.0
# pytest>=7.0.0
# mypy>=1.0.0
